Zdravím a pozdravuji,
kdyby jsi měl náhodou chvilinku času, nechtěl by jsi mi udělat málé code review? Problém je v tom, že místo aby to vypsalo:"Znak _ ve skutecnosti odpovida znaku_"
u všech malých písmen abecedy (tedy 26 myslím :) ), tak je výstup následující:
-------------------------------------------------
Znak b je ve skutecnosti znak s 
Znak c je ve skutecnosti znak e 
Znak d je ve skutecnosti znak r 
Znak e je ve skutecnosti znak q 
Znak i je ve skutecnosti znak h 
Znak j je ve skutecnosti znak y 
Znak n je ve skutecnosti znak p 
Znak q je ve skutecnosti znak k 
Znak q je ve skutecnosti znak x 
Znak s je ve skutecnosti znak j 
Znak t je ve skutecnosti znak v 
Znak w je ve skutecnosti znak k 
Znak w je ve skutecnosti znak x 
------------------------------------------------

Kodík vypadá takto:
------------------------------------------------
#include <stdio.h>
#include <stdlib.h>

int main()
{
    char hledany_znak = 'a';
    char znaky_textu = '0';
    char znak = 'a';
    float pocet = 0.000000;
    float vyskyt = 0.000000;
    double zastoupeni = 0.000000;
    float pole[26];
    float procenta = 0.000000;
    int a = 0;
    int j = 0;
    int i = 0;

    FILE *t;
    FILE *f;
    FILE *g;
    FILE *h;
    FILE *e;

    t = fopen("textnadesifraci.txt", "r");
    f = fopen("statistika.txt", "r");
    g = fopen("cvicnytext.txt", "r");
    h = fopen("vyskyt.txt", "w");
    e = fopen("sifraklic.txt", "w");

    for(i = 0; i <= 25; i++)
    {
       while(fscanf(g,"%c", &znaky_textu) == 1)
        {
            if(((znaky_textu >= 'a') && (znaky_textu <= 'z')) || ((znaky_textu >= 'A') && (znaky_textu <= 'Z')))
            {
                pocet++;

                if(znaky_textu == hledany_znak)
                {
                    vyskyt++;
                }

                else if(znaky_textu == (hledany_znak - 32))
                {
                    vyskyt++;
                }

            }
        }

       zastoupeni = ((100 * vyskyt) / pocet);
       fprintf(h,"Znak %c ma zastoupeni %f \n", hledany_znak, zastoupeni);

       pole[i] = zastoupeni;

       zastoupeni = 0.000000;
       vyskyt = 0.000000;
       pocet = 0.000000;
       znak = 'a';

       hledany_znak = hledany_znak + 1;
       fseek(g,0,SEEK_SET);
    }

    pole[26] = '\0';
    hledany_znak = 'a';
    a = 0;

    for(j = 0; j <= 25;j++)
    {
       if(j == 0)
       {
          fseek(f,+4,SEEK_CUR);
       }

       else
       {
           fseek(f,+7,SEEK_CUR);
       }

       fscanf(f,"%f", &procenta);

       do{
           if(pole[a] == procenta)
           {
               znak = znak + a;
               fprintf(e,"Znak %c je ve skutecnosti znak %c \n",hledany_znak,znak);
            }
           a++;
         }while(a <= 25);

       hledany_znak = hledany_znak + 1;
       znak = 'a';
       a = 0;
    }

    fclose(f);
    fclose(g);
    fclose(h);
    fclose(t);
    fclose(e);

return 0;
}
---------------------------------------------------

Předenm díky za jakékoliv nározy a připomínky

Bud dobrý!
